# Home Assistant Add-on: AppDaenon

## Options

- `server` (required): this should be the local URL on which the AppDaemon frontend is running, e.g. `http://192.168.2.43:8080`. Make sure there is no trailing slash!